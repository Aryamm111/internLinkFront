import React from 'react';
import { useUser } from '../UseContext.jsx';

const TopNavBar = () => {
  const { userName } = useUser();
  <div>
  <header className="bg-white shadow p-4 border-b border-gray-200 w-full fixed top-0 z-10">jk</header>
  </div>
};

export default TopNavBar;
